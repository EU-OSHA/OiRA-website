display:
  default:
    display_options:
      exposed_form:
        options:
          exposed_sorts_label: 'Panta:'
      fields:
        field_tool_link:
          alter:
            text: 'Að fá aðgang að tólinu'
            alt: 'Að fá aðgang að tólinu'
      sorts:
        field_publication_date:
          expose:
            label: 'Nýjasta fyrst'
        title:
          expose:
            label: 'Í stafrófsröð'
      header:
        area:
          content:
            value: "<div class=\"results-for\">Results for <span class=\"searched-text\">\"{{ filters.search_api_fulltext }}\" </span></div>\r\n"
            format: full_html
        result:
          content: '<div class="global summary">Sýnir @start - @end frá @total</div>'
  tools_ws:
    display_options:
      filters:
        search_api_fulltext:
          expose:
            placeholder: Leita
